# Fendix v0.21 Task Manifest — Earn Trust
Generated by: Orchestrator Agent
Date: 2026-06-28
Phase: v0.21 — Earn Trust (Foundation)
Exit criteria: *"Zero unresolved HIGH security issues. Every security claim documented and reproducible."*

## ⚠️ Critical scoping finding — most HIGH issues are ALREADY fixed

The `tasks/enterprise-readiness/` sprints (01–18, complete) already closed the
big HIGH findings from `ENTERPRISE_REVIEW_2026-06-07.md`. v0.21 must **verify
and lock** these (regression tests), **close the genuine remaining gaps**, and
**ship the trust docs** — NOT re-fix done work (Rule 1, Rule 10).

| HIGH finding | State (verified 2026-06-28) | v0.21 action |
|--------------|------------------------------|--------------|
| F-H1 SSRF guard | ✅ FIXED — `netguard` blocks metadata IP / RFC1918 / loopback / link-local + DNS-rebind, applied to all scanner clients | Add a regression test that locks the guard (esp. 169.254.169.254) |
| F-H2 Plugin sandboxing | ✅ SUBSTANTIALLY FIXED — repo-local opt-in OFF by default; symlink/ownership/perm pre-exec checks; env allowlist | Regression test for default-off + symlink rejection |
| F-H3 GitHub-App token | ✅ FIXED — `git -c http.extraheader` (not `.git/config`); env scrubbed for child | Regression test asserting token never in `.git/config` |
| F-H5a Webhook async | ✅ FIXED — bounded worker pool + dedup; 200 returned immediately | (covered by existing ghapp tests) |
| **F-H4 `--offline` no-op** | ❌ OPEN — flag registered but dep scanners still hit osv.dev/vuln.go.dev; docs claim "zero outbound" | **FIX:** wire `--offline` to gate outbound dep lookups; correct the docs claim |
| **Config-leak FP** | ❌ OPEN — `configleak.go` flags any 2xx without body validation → phantom CRITICALs on SPA catch-all (found in v0.20) | **FIX:** validate body isn't the SPA/HTML fallback before flagging |
| **F-H5b AST recursion bomb** | ❌ OPEN (Python) — `_trace_to_source()` has no depth cap; a crafted file can suppress all injection findings | **FIX:** add a depth/visited cap to the taint tracer |
| F-M1/F-M2 install.sh verify | ⚠️ PARTIAL — releases cosign-signed, but install.sh verifies a same-origin checksum (no cosign) and silently skips if tools absent | **FIX:** fail-closed checksums + optional cosign verification |
| Audit logging | ⚠️ MINIMAL — INFO logs only, no structured trail | Document as known follow-up (not a HIGH) |

## Task list (proposed — pending scope confirmation)

| ID | Task | Risk | File(s) |
|----|------|------|---------|
| V1 | **Config-leak FP fix** — suppress when 200 body is an HTML/SPA fallback (Content-Type `text/html` OR sniffed HTML); TDD: SPA-fallback fixtures yield no finding, plaintext `.env`/git-HEAD still fire. **DONE** | MED (scan logic) | `go/internal/scanner/configleak.go` (+test) |
| V2 | **Wire `--offline`** — gate pip/npm/govulncheck outbound on `cfg.Offline`; correct README "zero outbound" wording; TDD: offline scan makes no network call | MED (scan logic) | `go/internal/engine/orchestrator.go`, dep scanners, `README.md` |
| V3 | **AST taint recursion cap** (F-H5b) — depth + visited-set guard; TDD: bomb fixture still returns findings, bounded time | MED (Python) | `python/analyzers/ast_analyzer.py` (+test) |
| V4 | **install.sh hardening** (F-M1/M2) — fail-closed if checksum tools absent; optional cosign verify with clear messaging | LOW (shipping) | `scripts/install.sh` |
| V5 | **Regression locks** for the already-fixed HIGH issues (SSRF metadata IP, plugin default-off, token not in `.git/config`) | LOW (tests only) | `go/internal/{netguard,plugin,ghapp}/*_test.go` or `go/tests/security/` |
| V6 | **Trust docs** — Trust Center, threat model (reconcile existing `docs/threat-model.md`), privacy doc, responsible disclosure (extend `SECURITY.md`) | LOW (docs) | `docs/`, `SECURITY.md` |
| V7 | Security re-audit of all v0.21 changes | — | `SECURITY_AUDIT_v021.md` |
| V8 | Reviewer: exit criteria (zero unresolved HIGH) + Constitution | — | `REVIEW_v021.md` |
| V9 | Orchestrator close + `NEXT_v022.md` | — | manifest + handoff |

## Ambiguities / decisions for the owner
1. **Scope.** Minimal (V5 regression locks + V6 docs → "HIGH fixed & documented") vs Thorough (also V1–V4 close the genuine gaps). Recommend **Thorough** — v0.20's whole lesson was that the gaps are real (the config-leak FP fires on most SPA apps).
2. **Python in scope?** V3 (F-H5b) touches `python/`. The roadmap's v0.21 is engine-wide, so yes — but flag it as the only Python change.
3. **Config-leak fix approach (V1).** Body-difference heuristic (compare to a known-404/catch-all baseline) vs content-type/signature allowlist. The fix must not regress true positives (a real exposed `.env` is plaintext, not HTML).
