# Fendix v0.24 Task Manifest — Decision Reports
Generated by: Orchestrator Agent
Date: 2026-06-28
Phase: v0.24 — Decision Reports (Core product)
Exit criteria: *"Engineers see decisions, not just findings. PR blocking works. SARIF compatibility intact."*
Success metrics: PR blocking works on `BLOCK` findings · report generation adds < 200ms · SARIF output validated.

## Governing invariant (NEW for this phase)
v0.20–v0.23 were output-stable. **v0.24 is the FIRST phase that intentionally
changes user-facing output** (decision summary + visible confidence). So:
- The v0.20 `tests/regression/output_format` snapshots **will change** — update
  them DELIBERATELY (`FENDIX_UPDATE_SNAPSHOTS=1`) and review every diff.
- The raw `findings[]` JSON schema stays **backward-compatible** — the decision
  summary + confidence are ADDED as new fields/sections, existing fields/types
  unchanged (guarded by `reporters/schema_test.go`, which gets extended, not broken).

## Already built (build on — Rule 1)
- `decision.Decision{Status BLOCK/WARN/INFO/IGNORE, Confidence, Score{Value,Band,Reasons}, Evidence}` + `Decide`/`DecideAll`/`ExitCode` (exit-code locked to `checkFailOn`).
- `confidence.Score` (deterministic 0–100 + reasons).
- Evidence provenance/lineage threaded through correlation; `models.Finding` carries Source/Reachable/ProvenPath/etc.

## Roadmap deliverables → task plan (refined after the understand blueprint)
| ID | Task |
|----|------|
| D1 | Compute per-finding decision + confidence in the orchestrator (after finalization) |
| D2 | JSON: add a decision SUMMARY block (total → confirmed → blocking → informational) + per-finding confidence — additive/backward-compatible |
| D3 | SARIF decision status (result properties) + HTML decision section |
| D4 | CLI human-readable decision summary (stderr; stdout stays machine-readable) |
| D5 | GitHub Action PR annotations with decision status + PR blocking on BLOCK |
| D6 | Deliberately update v0.20 output snapshots + extend schema_test + new tests |
| D7 | Adversarial review (multi-lens verify: schema back-compat, SARIF validity, blocking semantics, perf <200ms) |
| D8 | Security audit + review + close + `NEXT_v025.md` |

## Status
Understand workflow running (maps surfaces → implementation blueprint); task plan above is refined once it lands.
