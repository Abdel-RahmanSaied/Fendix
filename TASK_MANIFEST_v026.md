# Fendix v0.26 Task Manifest — Accuracy & Benchmarking
Generated by: Orchestrator Agent
Date: 2026-06-30
Phase: v0.26 — Accuracy & Benchmarking (Core product)
Exit criteria: *"Honest, reproducible, published accuracy numbers. No marketing claim without a re-runnable benchmark."* (Product Constitution Rule 5)

## Governing principle
Turn Fendix's accuracy story from **assertion** into **evidence**. Every
published number (recall / precision / F1 / FP-rate) must be reproducible by a
third party: pinned corpus, documented methodology, deterministic scoring,
ideally re-checked in CI. Rule 8: AI never decides a score or a label.

## Hard constraints (the honesty that defines this phase)
- **OWASP Benchmark is Java; Fendix has no Java analyzer until v0.27.** It MUST
  keep loud-SKIPping — never publish a fake ~0-recall number from it (that is
  itself a Rule 5 violation — a misleading baseline). DAST accuracy = DVWA +
  Juice Shop (HTTP, language-agnostic); SAST accuracy = the `scripts/heavy-eval`
  Python/Go corpus.
- **No number without a reproducible source.** Any EXISTING unsubstantiated
  accuracy/marketing claim (in README/docs/comments) is a live Rule 5 violation
  → MUST be backed by a real number or removed/softened.
- **Scan-report JSON schema stays stable.** Output may grow a `BENCHMARKS.md` +
  README numbers; the v0.20 snapshots (project category|severity|title) stay
  green; any JSON change is additive + schema-lockstep.

## Already built (build on — Rule 1)
- Benchmark harness: `go/internal/benchmark/{baseline,runner}.go` + `fendix benchmark`.
- Targets: `go/internal/benchmark/targets/{dvwa,juiceshop,owasp,docker}.go` (OWASP loud-skips).
- SAST corpus: `scripts/heavy-eval/{run,score,gate,corpus,perf}.py` + `labels/`.

## Roadmap deliverables (refined to a batch plan after the understand blueprint)
- A published, reproducible **BENCHMARKS.md** (methodology + numbers + how to re-run).
- README accuracy numbers backed by the harness (not assertions).
- Fix every live unsubstantiated accuracy claim found in the audit.
- DAST recall/precision on DVWA + Juice Shop; SAST recall/precision/F1 on heavy-eval.
- OWASP/Java skip handled honestly + documented; Java deferred to v0.27.
- (If gaps) solidify ground-truth labels + deterministic scoring + a CI accuracy check.

## Won't Do (defer to v0.27+)
- Java analyzer + OWASP Benchmark real run. Any corpus needing a language Fendix can't yet analyze.

## ✅ PHASE v0.26 — COMPLETE (2026-06-30)

Adversarial 6-lens review → FIX-FIRST → all addressed → **SHIP**; Reviewer → **SHIP**.
A truth-reconciliation phase: the headline accuracy numbers were pinned to v0.11.0 and no
longer reproduced (live Rule 5 violations). Reproduced the real numbers on the current binary
and reconciled every public claim.

- **Verified ground truth:** Python taint engine P/R/F1 = 1.000 (40 cases, CI-gated); SAST
  synthetic P 1.000 / R 0.974 / **F1 0.987** (one disclosed SSRF multi-hop FN); DAST DVWA 13/0,
  Juice Shop 12/5 (regression coverage; no FP-rate).
- **B0/B4 (99238b7):** NEW BENCHMARKS.md (every number → reproduce command + stamp + caveat +
  CI-flag; OWASP documented as SKIPPED); reconciled MF-1/2/5/7 across README/accuracy.md/
  marketplace/launch-post.
- **B3+MF-3 (7a3c0e1):** `--min-f1` gate + push/PR CI wiring (closes the drift hole); gate
  docstring 0.90 → enforced 0.95.
- **Review fixes (00875b1):** corpus count 56/38 (not 55/37); swept the missed operator-rollout.md
  (1.000 → 0.987 in two places); marketing-plan recall banner; bootstrap-RNG determinism;
  NIST-framing comments; count/stamp nits.

We publish 0.987, not a rounded 1.000. Audit: `SECURITY_AUDIT_v026.md`. Review: `REVIEW_v026.md`.
Handoff: `NEXT_v027.md` (Java analyzer → unlocks OWASP; + deferred accuracy hardening).
Deferred (honest): SSRF multi-hop taint fix, B1/B2 (DAST scorer/repro), B5 (corpus quality),
MF-4/MF-6/MF-8.
