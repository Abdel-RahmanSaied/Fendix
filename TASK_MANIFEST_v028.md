# Fendix v0.28 Task Manifest — Java SAST coverage (increment 2)
Generated by: Orchestrator Agent
Date: 2026-06-30
Phase: v0.28 — toward deep Java taint (bounded, honest increment)
Exit criteria: *"Broader, real Java SAST coverage Fendix can honestly claim — advancing toward the Java-taint goal without faking the quarter-scale engine or claiming an OWASP number."*

## The honesty constraint that shapes this version (read first)
The roadmap's headline next item is a **deep Java taint analyzer**. Two hard
facts (confirmed in plan.md + the v0.27 review):
- It is **quarter-scale** ("tree-sitter port is a quarter's work on its own",
  plan.md:48) — not completable to production/OWASP quality in one session.
- It needs a **Java-parser architecture decision** (CGo tree-sitter — which
  breaks Fendix's single-static-binary identity — vs a separate engine vs
  shelling out). That is an **operator-owned** call, not a unilateral one.
- A shallow taint engine that then claims OWASP numbers is the exact Rule 5
  violation v0.26/v0.27 spent two phases removing.

So v0.28 ships a **real, bounded, reviewable** increment — expanded Java regex
SAST coverage (line-local, `TierNativeGo`, the same honest tier as v0.27) —
and explicitly **DEFERS** the deep taint engine + the parser decision + any
OWASP number to a future phase with a crisp definition-of-done.

## Governing invariants
- No Java/OWASP accuracy number (no labeled Java corpus). OWASP stays the
  two-layer SKIP from v0.27.
- Java findings stay at `TierNativeGo` (regex), never a taint/tree-sitter tier.
- Scan-report JSON schema stable; additive output only; Rule 8 (no AI); Rule 1
  (build on the textscan rule mechanism, no rewrite).

## Won't Do (this phase — deferred with definition-of-done)
- A deep Java taint engine (interprocedural, sanitizer-aware source→sink).
- The Java-parser architecture decision (operator-owned).
- Any OWASP Benchmark number.

## Status
Understand workflow (`ws6q4vord`) running → decides the exact bounded increment
(high-value line-local Java rules, e.g. XXE / path-traversal / SSRF / LDAP /
weak-RNG / XSS, + FP guards; possibly OWASP corpus scaffolding behind the SKIP).
Plan finalized once it lands.
