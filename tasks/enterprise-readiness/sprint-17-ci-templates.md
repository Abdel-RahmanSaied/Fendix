# Sprint 17 — GitLab + CircleCI templates in `fendix init`

**Phase:** 6.2 | **Estimate:** 1 day | **Risk:** Low | **Ships:** v0.14.0
**Audit ref:** [`FENDIX_AUDIT_REPORT.md`](../../FENDIX_AUDIT_REPORT.md) §8 (GitHub Actions only today)

---

## Why

Enterprise customers run on multiple CI systems. Today `fendix init` only emits a GitHub Actions workflow. This sprint adds GitLab CI and CircleCI templates.

---

## Read first

- [`go/internal/initcmd/init.go`](../../go/internal/initcmd/init.go) — the orchestrating logic
- [`go/internal/initcmd/templates/workflow.yml`](../../go/internal/initcmd/templates/workflow.yml) — current GH Actions template
- [`go/internal/initcmd/detect.go`](../../go/internal/initcmd/detect.go) — project-type detection. Extend with CI-system detection.

---

## New templates

### `go/internal/initcmd/templates/gitlab-ci.yml`

```yaml
# Fendix security scan — GitLab CI
#
# Generated by `fendix init --ci gitlab`.
# Produces a GitLab SAST report uploaded as a CI artifact.

fendix-security-scan:
  stage: test
  image: alpine:latest
  variables:
    FENDIX_VERSION: "v0.14.0"  # pin to a tagged release
  before_script:
    - apk add --no-cache curl ca-certificates
    - curl -fsSL "https://github.com/Abdel-RahmanSaied/Fendix/releases/download/${FENDIX_VERSION}/fendix-linux-amd64.tar.gz" | tar xz -C /usr/local/bin
    - chmod +x /usr/local/bin/fendix
  script:
    - fendix scan --code . --format sarif --output gl-sast-report.json --fail-on HIGH
  artifacts:
    when: always
    reports:
      sast: gl-sast-report.json
    paths:
      - gl-sast-report.json
    expire_in: 30 days
  allow_failure: false
```

### `go/internal/initcmd/templates/circleci-config.yml`

```yaml
# Fendix security scan — CircleCI
# Generated by `fendix init --ci circleci`.

version: 2.1

jobs:
  fendix-scan:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Install Fendix
          command: |
            FENDIX_VERSION="v0.14.0"
            curl -fsSL "https://github.com/Abdel-RahmanSaied/Fendix/releases/download/${FENDIX_VERSION}/fendix-linux-amd64.tar.gz" | sudo tar xz -C /usr/local/bin
      - run:
          name: Run Fendix scan
          command: fendix scan --code . --format sarif --output fendix-report.json --fail-on HIGH
      - store_artifacts:
          path: fendix-report.json

workflows:
  security:
    jobs:
      - fendix-scan
```

## CLI flag

```
fendix init [--ci github|gitlab|circleci]
```

Auto-detect default:
- If `.github/` exists → `github`
- Else if `.gitlab-ci.yml` exists → `gitlab`
- Else if `.circleci/` exists → `circleci`
- Else → `github` (most common)

## File paths emitted

| `--ci` | Path |
|---|---|
| `github` | `.github/workflows/fendix.yml` (current behaviour) |
| `gitlab` | `.gitlab-ci.fendix.yml` (a separate file the user `include:`s from their main `.gitlab-ci.yml`) |
| `circleci` | `.circleci/fendix-config.yml` (orb-style snippet the user merges into their main config) |

For gitlab/circleci, also emit a `NEXT-STEPS.md` explaining how to wire the snippet into the user's main CI config (since CircleCI and GitLab don't merge multiple config files automatically the way GH Actions does).

## Update detect.go

```go
type DetectionResult struct {
    ProjectType string  // existing
    CIType      string  // NEW: "github" | "gitlab" | "circleci" | "" (unknown)
}

func DetectCI(rootPath string) string {
    if exists(filepath.Join(rootPath, ".github")) { return "github" }
    if exists(filepath.Join(rootPath, ".gitlab-ci.yml")) { return "gitlab" }
    if exists(filepath.Join(rootPath, ".circleci")) { return "circleci" }
    return ""
}
```

## Tests

```go
// TestDetectCI_GitHub — fixture with .github/, no others → "github"
// TestDetectCI_GitLab — fixture with .gitlab-ci.yml → "gitlab"
// TestDetectCI_CircleCI — fixture with .circleci/ → "circleci"
// TestDetectCI_NoCIDetected → ""
// TestInit_GitLab_WritesCorrectFiles — temp dir, `init --ci gitlab`, assert files present + parse as YAML
// TestInit_CircleCI_WritesCorrectFiles — same for circleci
// TestInit_AllTemplatesParse — every emitted YAML round-trips through gopkg.in/yaml.v3
// TestInit_NextStepsFileMentionsInclude — gitlab/circleci NEXT-STEPS.md tells user how to wire
```

## CHANGELOG

```markdown
### Added (v0.14.0)

- **GitLab CI and CircleCI templates** in `fendix init`. New flag
  `--ci github|gitlab|circleci` (auto-detected from project files
  if not specified).
  - `github` (default): `.github/workflows/fendix.yml`
  - `gitlab`: `.gitlab-ci.fendix.yml` + `NEXT-STEPS.md`
  - `circleci`: `.circleci/fendix-config.yml` + `NEXT-STEPS.md`
```

---

## Risks

- **GitLab SAST report schema changes occasionally.** Pin the schema version in the template's `# generated against schema X.Y` comment.
- **CircleCI orb vs. inline config.** This sprint uses inline; an orb would be cleaner but requires CircleCI's orb-publication process.

## Definition of done

Standard DoD plus:
- Each emitted YAML parses cleanly with `gopkg.in/yaml.v3`
- `fendix init --ci gitlab` against a fresh repo produces output that GitLab CI's syntax validator accepts (manual verify)
- README mentions multi-CI support

## Follow-ups

- **Sprint 17.5:** Bitbucket Pipelines, Azure DevOps templates
- **Sprint 17.6:** Publish a CircleCI orb (separate registration)

## Status

**Started:** 2026-05-15 (AI implementer, plan-finish session)
**Branch:** `plan-finish-phases-2-6`
**Status:** done
**Actual effort:** ~45 minutes vs 1-day estimate.

**Surprises:**

- **The brief's `gl-sast-report.json` is named for GitLab SAST report
  ingestion conventions, but the content is SARIF.** Used `--format
  sarif` in the template; GitLab Premium+ tiers consume the file as a
  SAST report via `reports.sast:`. Free tier still gets it as an
  artifact. No behaviour issue.
- **CircleCI's lack of multi-file config merge** means the gitlab/
  circleci paths both emit a `NEXT-STEPS-fendix.md` to explain wiring
  (`include:` for GitLab, manual merge for CircleCI). The brief
  treated this as a one-liner but it's load-bearing — a user who
  skips it sees no scan running and silently no findings.
- **README's existing CI section was thin.** Added a short multi-CI
  bootstrap snippet at the top of the section.

**Bench:** Sprint 17 only changed `init` (one-shot file emit) and
templates. `make bench` unaffected.

**Tests added:** 10 new tests in `init_ci_test.go`:
- `TestDetectCI_GitHub`, `TestDetectCI_GitLab`, `TestDetectCI_CircleCI`,
  `TestDetectCI_NoneDetected`, `TestDetectCI_GitHubPreferredOverOthers`
- `TestRun_GitLabWritesExpectedFiles`,
  `TestRun_CircleCIWritesExpectedFiles`
- `TestRun_AutoDetectsGitLab` (the full Run path with auto-detection)
- `TestRun_RejectsUnsupportedCI` (negative-path coverage for `--ci jenkins`)
- `TestAllTemplatesParseAsYAML` (every emitted `.yml` round-trips
  through `gopkg.in/yaml.v3` — guards against typos shipping in a
  release)
- `TestRun_PrintGitLabIncludesIncludeInstruction` (--print surfaces
  the `include:` instruction from NEXT-STEPS)

All existing initcmd tests still pass.

**Manual DoD evidence:**

```
$ bin/fendix init --ci gitlab          # in a fresh tmpdir
✓ Detected: Generic — no OpenAPI spec found at common paths
✓ CI system: gitlab
✓ Wrote .gitlab-ci.fendix.yml
✓ Wrote NEXT-STEPS-fendix.md
✓ Wrote .fendix.yaml
✓ Wrote .fendix-ignore

Next steps:
  git add .gitlab-ci.fendix.yml NEXT-STEPS-fendix.md .fendix.yaml .fendix-ignore
  git commit -m "Add Fendix security scanning"

Run a scan now:
  fendix scan --url https://api.example.com

Wiring instructions: see NEXT-STEPS-fendix.md (just written).
```

**Files touched:**

- `go/internal/initcmd/init.go` — refactor to dispatch by CI system,
  add `Options.CI`, `SupportedCIs`, `ErrUnsupportedCI`, `resolveCI`.
  ~80 LOC added.
- `go/internal/initcmd/detect.go` — `DetectCI()` + `isDir`/`isFile`
  helpers. ~25 LOC added.
- `go/internal/initcmd/templates/gitlab-ci.yml` — NEW.
- `go/internal/initcmd/templates/circleci-config.yml` — NEW.
- `go/internal/initcmd/templates/next-steps-gitlab.md` — NEW.
- `go/internal/initcmd/templates/next-steps-circleci.md` — NEW.
- `go/internal/initcmd/init_ci_test.go` — NEW, 10 tests.
- `go/cmd/fendix/main.go` — `--ci` flag on `init` subcommand;
  Long-doc explains all three CIs.
- `README.md` — multi-CI bootstrap snippet in the CI/CD section.
- `CHANGELOG.md` — v0.14.0 Sprint-17 entry.
- `tasks/enterprise-readiness/PLAN.md` — Sprint 17 ✅.

**Follow-ups created:**

- The two from the sprint file's existing Follow-ups (Bitbucket
  Pipelines + Azure DevOps templates as Sprint 17.5; CircleCI orb as
  Sprint 17.6) remain open.

**Hard-rule compliance:** No new deps. No CGo. No CLI-flag renames
(only added `--ci`). No `.fendix.yaml` key changes. No Finding-struct
changes. `.gitkeep` build artifact left uncommitted.
